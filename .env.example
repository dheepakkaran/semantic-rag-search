# Copy to .env and fill in. .env is gitignored — never commit real keys.

# gemini | openai | ollama | mock
LLM_PROVIDER=gemini

# Free key from https://aistudio.google.com
GEMINI_API_KEY=

# Only needed for LLM_PROVIDER=openai
# OPENAI_API_KEY=
