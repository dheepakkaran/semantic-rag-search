sentence-transformers>=3.0
numpy>=1.26
python-dotenv>=1.0
fastapi>=0.115
uvicorn[standard]>=0.30
pymongo>=4.8

# Generation providers. Gemini leads the default chain, OpenAI is the fallback.
google-genai>=1.0
openai>=1.0

# Optional: only needed for LLM_PROVIDER_CHAIN entries that use it.
# ollama>=0.3
